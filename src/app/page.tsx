export default function OverviewPage() {
  return (
    <main className="mx-auto flex w-full max-w-6xl flex-1 flex-col justify-center px-4 py-16 sm:px-6">
      <p className="text-xs font-medium uppercase tracking-wider text-muted-foreground">Marketing overview</p>
      <h1 className="mt-3 text-3xl font-semibold tracking-tight">Harbor House Inn</h1>
      <p className="mt-2 max-w-prose text-muted-foreground">The dashboard is being built. Data arrives once the database is seeded.</p>
    </main>
  );
}
